{
  "name": "monorepo-elysia-react-template",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun --cwd apps/web dev",
    "dev:api": "bun --cwd apps/api dev",
    "format": "bunx oxfmt .",
    "format:check": "bunx oxfmt --check .",
    "lint": "bunx oxlint --tsconfig tsconfig.base.json .",
    "lint:fix": "bunx oxlint --fix --tsconfig tsconfig.base.json .",
    "lint:fix:suggestions": "bunx oxlint --fix --fix-suggestions --tsconfig tsconfig.base.json .",
    "typecheck": "bun --cwd packages/commons typecheck && bun --cwd packages/migrations typecheck && bun --cwd apps/api typecheck && bun --cwd apps/web typecheck",
    "test": "bun --cwd apps/api test",
    "prepare": "husky",
    "db:migrate": "bun --env-file apps/api/.env --cwd packages/migrations run migrate",
    "db:generate": "cd apps/api && bunx kysely-codegen"
  },
  "devDependencies": {
    "bun-types": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "latest",
    "oxlint": "latest",
    "pino-pretty": "^13.1.3",
    "typescript": "latest"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "bunx oxfmt",
      "bunx oxlint --tsconfig tsconfig.base.json"
    ],
    "*.{json,md,css,html}": [
      "bunx oxfmt"
    ]
  }
}
